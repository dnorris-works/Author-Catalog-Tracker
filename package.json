{
  "name": "author-catalog-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm install --prefix web && npm run build --prefix web",
    "start": "npm run start --prefix web",
    "dev": "npm run dev --prefix web"
  },
  "engines": {
    "node": "20.x"
  }
}
